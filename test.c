#include <stdio.h>
#include <stdlib.h>
int main()
{
    // void *p = malloc(4);
    // printf("Address of p: %p\n", *(int*)p);
    // free(p);
    // int x = NULL;

    int *ptr = NULL;

    printf("Address of ptr: %p\n", ptr);

    return 0;
}